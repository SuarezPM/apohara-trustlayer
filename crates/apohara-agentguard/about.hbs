# Third-Party Licenses

This file lists the licenses of all third-party crates compiled into the
`apohara-agentguard` release binaries. It is generated by `cargo about generate` and
regenerated in CI on every release. apohara-agentguard itself is dual-licensed under
`MIT OR Apache-2.0` (see `LICENSE-MIT` and `LICENSE-APACHE`).

{{#each licenses}}
## {{name}}

Used by:
{{#each used_by}}
- {{crate.name}} {{crate.version}}
{{/each}}

```
{{text}}
```

{{/each}}
