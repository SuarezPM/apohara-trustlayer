# CODEOWNERS — OpenSSF Scorecard: Code-Review (10/10) + Branch-Protection
# (10/10). Branch protection requires at least 1 approval from a CODEOWNER
# on every PR to main. @SuarezPM is the BDFL; co-maintainers
#  and  are listed per SECURITY.md § Continuity.
#
# Pattern: every file in the repo has at least one owner. The default
# `*` rule catches anything not explicitly listed (defense in depth).
# Pin a GitHub team for faster review routing (Settings → Teams):
#   @SuarezPM/argus-maintainers
#
# After committing this file:
#   gh api -X PATCH repos/SuarezPM/apohara-argus/branches/main/protection \
#     -H "Accept: application/vnd.github+json" \
#     -f required_pull_request_reviews[require_code_owner_reviews]=true
# (the maintainer runs this once; CI does not need it).

# Default: any change needs a CODEOWNER review.
/*                                       @SuarezPM 

# Critical paths: BDFL must review explicitly (per the dual-control
# rule in SECURITY.md § Access Continuity).
/Cargo.toml                               @SuarezPM
/Cargo.lock                               @SuarezPM
/SECURITY.md                              @SuarezPM 
/GOVERNANCE.md                            @SuarezPM 
/.github/workflows/release.yml            @SuarezPM
/.github/workflows/publish.yml            @SuarezPM
/.github/workflows/publish-crates.yml     @SuarezPM
/.github/workflows/_attest.yml            @SuarezPM
/deny.toml                                @SuarezPM
/crates/argus-crypto/                     @SuarezPM
/crates/argus-verify/src/cordon.rs        @SuarezPM 

# Audit chain code: the BLAKE3 + Ed25519 chain is the regulator-facing
# artifact. Any change here is breaking.
/crates/apohara-argus-core/src/types.rs   @SuarezPM 
/crates/argus-crypto/                     @SuarezPM 

# Threat model + security docs: BDFL + co-maintainer must both sign off.
/docs/THREAT_MODEL.md                     @SuarezPM 
/docs/ASSURANCE_CASE.md                   @SuarezPM 
/docs/SECURITY.md                         @SuarezPM 

# CI: CODEOWNER review on workflow changes (defense in depth).
/.github/workflows/                       @SuarezPM
/.github/dependabot.yml                   @SuarezPM
/.github/CODEOWNERS                       @SuarezPM 
